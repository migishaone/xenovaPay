# PawaPay API Configuration
# Get your API token from https://dashboard.pawapay.cloud/
PAWAPAY_API_TOKEN=your-pawapay-api-token-here

# PawaPay API Base URLs (usually no need to change these)
PAWAPAY_API_BASE=https://api.sandbox.pawapay.io/v2
PAWAPAY_WIDGET_API_BASE=https://api.sandbox.pawapay.cloud/v1

# Application Configuration
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000

# For production deployment, update BASE_URL to your actual domain:
# BASE_URL=https://your-domain.com

# To run the app independently (without Replit dependencies):
# Development: PORT=3000 NODE_ENV=development tsx server/index.standalone.ts
# Production: tsx scripts/build-client.ts && NODE_ENV=production tsx server/index.standalone.ts